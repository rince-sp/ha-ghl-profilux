{
  "config": {
    "step": {
      "user": {
        "title": "Connect to ProfiLux",
        "description": "Choose an interface. GHL API (recommended, firmware 7.31+) — enable it first under System → GHL API in GHL Control Center / GHL Connect; talks the API over its WebSocket, gives richer data and per-sensor level states, but is read-only (no socket switching). HTTP / WebSocket — the raw SWMBus interface, which also allows socket on/off control.",
        "data": {
          "host": "Host (IP address)",
          "username": "Username (SWMBus only)",
          "password": "Password (SWMBus only)",
          "interface": "Interface"
        }
      }
    },
    "error": {
      "cannot_connect": "Failed to connect. Check the host, credentials and interface."
    },
    "abort": {
      "already_configured": "This device is already configured."
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "ProfiLux options",
        "description": "Socket control writes to the controller and can override the automatic control of live equipment. Leave it off unless you want switches.",
        "data": {
          "control_sockets": "Enable socket control (adds an on/off switch per socket)"
        }
      }
    }
  }
}
